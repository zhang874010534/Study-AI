[project]
name = "llmops-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.3",
    "injector>=0.24.0",
    "langchain>=1.3.4",
]

[tool.setuptools.packages.find]
where = ["."]
exclude = ["test*", ".venv*", ".*"]
